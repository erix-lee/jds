package com.cnupa.app.repo;

import org.springframework.data.jpa.repository.JpaRepository;

import com.cnupa.app.model.CoursewareShare;

public interface CoursewareShareRepo extends JpaRepository<CoursewareShare, Long>{
}